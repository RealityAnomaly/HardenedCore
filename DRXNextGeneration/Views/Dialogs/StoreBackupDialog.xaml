<ContentDialog
    x:Class="DRXNextGeneration.Views.Dialogs.StoreBackupDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:DRXNextGeneration.Views.Dialogs"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="Store Export"
    PrimaryButtonText="Export"
    CloseButtonText="Cancel"
    PrimaryButtonClick="ContentDialog_PrimaryButtonClick">

    <StackPanel VerticalAlignment="Stretch" HorizontalAlignment="Stretch">
        <StackPanel>
            <TextBlock Text="Backup Security Type" />
            <RadioButton x:Name="FullCryptoRadioButton" Content="Full Encryption" IsChecked="True" />
            <RadioButton x:Name="EscrowCryptoRadioButton" Content="Full Encryption with Escrow Key" />
            <RadioButton x:Name="NoCryptoRadioButton" Content="No Encryption" />
        </StackPanel>
    </StackPanel>
</ContentDialog>
