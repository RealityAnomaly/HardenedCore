<Page
    x:Class="DRXNextGeneration.Views.ExtendedSplash"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:DRXNextGeneration.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid Background="#000000">
        <Canvas>
            <Image x:Name="ExtendedSplashImage" Source="../Assets/SplashScreen.scale-200.png" />
            <StackPanel x:Name="ExtendedSplashProgress" HorizontalAlignment="Center" Width="300" Height="200" Margin="0,40,0,0">
                <TextBlock Text="Please be patient." FontSize="20" HorizontalAlignment="Center" />
                <ProgressBar IsIndeterminate="False" x:Name="LoadingBar" Margin="0,10,0,0" />
                <TextBlock Text="Please wait." FontSize="15" HorizontalAlignment="Center" x:Name="LoadingStatus" Margin="0,10,0,0" />
            </StackPanel>
        </Canvas>
    </Grid>
</Page>
